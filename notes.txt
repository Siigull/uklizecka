Add seeding data

Logging of everything.
    - have commands to show which are missing
    - Visualize table in discord without needing the website

 Finished might be needed for everyone, so that there arent issues with
needing to ask every other person if they can confirm it.

 Maybe have a button for individual finish and total.

TODO(Sigull): Should have two levels of logging. Unassigning is important.
TODO(Sigull): Feature(Splitting cleaning into smaller parts).
TODO(Sigull): Show all templates and their instructions command.

TODO(Sigull): Maybe when cleaning starts users cant leave.

TODO(Sigull): Have commands for manager to handle leave when started etc.

node --env-file=.env main.js
